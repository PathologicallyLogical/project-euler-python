from math import *

def isPrime(n):
    for i in range(2,int(n**0.5)+1):
        if n%i==0:
            return False
    return True


def factor(x):
    factors = []
    store = x
    while store > 1:
        for i in range(2,x):
            if store%i == 0 and isPrime(i):
                if not(i == 1):    
                    factors.append(i)
                    store /= i
        if store == 1:
            store = 0
    factors.sort()
    return factors
def final(num):
    look_up = {}
    for f in range(num):
        look_up[f] = 0
    print look_up
    for i in range(num):
        #loop through num
        arr = factor(i)
        #factor i, return factor array
        print "arr for "+str(i)+"is "+str(arr)
        """for l in arr:
            #loop thru factor array
            store = arr.count(l)
            if look_up[l] <= store:
                look_up[l] = store"""
